<!DOCTYPE html>
<html>
<head>
    <title>{{.RepoName}} - GoGit</title>
    <style>
        body { font-family: sans-serif; margin: 40px; color: #333; }
        .breadcrumb { font-size: 1.2rem; margin-bottom: 20px; }
        .file-table { width: 100%; border-collapse: collapse; border: 1px solid #ddd; }
        .file-table th, .file-table td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        .file-table tr:hover { background-color: #f5f5f5; }
        .icon { margin-right: 10px; }
        .btn-back { text-decoration: none; color: #0366d6; }
    </style>
</head>
<body>
    <div class="breadcrumb">
        <a href="/dashboard" class="btn-back">Dashboard</a> / 
        <strong>{{.Username}}</strong> / {{.RepoName}}
    </div>

    <table class="file-table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody>
            {{range .Files}}
            <tr>
                <td>
                    <span class="icon">{{if .IsFile}}📄{{else}}📁{{end}}</span>
                    <a href="#">{{.Name}}</a>
                </td>
                <td>{{if .IsFile}}File{{else}}Directory{{end}}</td>
            </tr>
            {{else}}
            <tr>
                <td colspan="2">This repository is empty.</td>
            </tr>
            {{end}}
        </tbody>
    </table>
</body>
</html>